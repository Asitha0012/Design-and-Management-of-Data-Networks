# Ansible Configuration Settings for Network Device Management

[defaults]
# Default inventory path for target devices
inventory = inventory.ini

# Disable SSH host key checking for lab/automated network environments
host_key_checking = False

# Disable creation of retry files (.retry) on playbook failure
retry_files_enabled = False
